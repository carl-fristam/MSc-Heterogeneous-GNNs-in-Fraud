model:
  hidden_channels: 64
  num_layers: 2
  dropout: 0.5

training:
  epochs: 100
  lr: 0.01
  weight_decay: 5.0e-4
  batch_size: 1024
  num_neighbors: [10, 5]

data:
  dataset: HI-Small
  sample_ratio: 1.0

splits:
  train_ratio: 0.6
  val_ratio: 0.2
