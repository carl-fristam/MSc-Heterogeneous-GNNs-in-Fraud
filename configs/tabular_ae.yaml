model:
  h1: 64
  h2: 32
  latent_dim: 16
  dropout: 0.2

training:
  epochs: 200
  lr: 1.0e-3
  weight_decay: 1.0e-5
  batch_size: 512
  patience: 20
  scheduler: true

data:
  sample_ratio: 0.1    # ~950K transactions; set to 1.0 for final runs
